// Legacy/non-standalone component stub kept to avoid duplicate compilation
// The real application bootstraps the standalone `App` component in `app/app.ts`.

export const __APP_COMPONENT_STUB__ = {};
